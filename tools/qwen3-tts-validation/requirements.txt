# Qwen3-TTS 验证环境依赖（conda 通用版本）
# 建议使用 Python 3.10+

# Qwen3-TTS 官方 Python 包（包含模型定义和推理代码）
qwen-tts>=0.1.1

# transformers 版本必须固定，>=5.0 会破坏 Qwen3-TTS 兼容性
transformers==4.57.3

# 核心框架
torch>=2.1.0
accelerate>=0.25.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# 音频处理
soundfile>=0.12.1
librosa>=0.10.1

# ONNX 导出与推理
onnx>=1.16.0
onnxscript>=0.2.0
onnxruntime>=1.18.0
onnxruntime-gpu>=1.18.0; platform_system=="Windows" and platform_machine=="AMD64"

# 量化工具（可选）
onnxruntime-tools>=1.7.0

# 模型下载
huggingface-hub>=0.24.0

# 其他工具
tqdm>=4.66.0
numpy>=1.24.0
